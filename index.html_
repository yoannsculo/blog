---
layout: default
---

<div class="container">
	<div class="well">
	<h2>Articles</h2>
	{% for post in site.posts|reverse %}
		{{ post.date|dateFormat }} » <a href="{{ post.url }}">{{ post.title }}</a><br>
	{% endfor %}
	</div>
</div>
