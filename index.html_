---
layout: default
---

<div class="container">
	<div class="well">
	<h2>Articles</h2>
	{% for post in site.posts|reverse %}
		<div style="margin-bottom:5px;"><span class="label label-info">{{ post.date|dateFormat }}</span> » <a href="{{ post.url }}">{{ post.title }}</a><br></div>
	{% endfor %}
	</div>
</div>
